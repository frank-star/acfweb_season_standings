const initialState = {
  isFetching: false,
  isLogged: true,
  details: {}
}

export default (state = initialState, action) => {
  switch (action.type) {
    // Auth types

    default:
      return state
  }
}
